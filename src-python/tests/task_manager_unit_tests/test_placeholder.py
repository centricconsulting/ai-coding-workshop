"""Placeholder unit test — replaced by real Task/TaskId/TaskStatus tests in Lab 1.

This exists only so `pytest` has something to collect and run as a sanity
check (e.g. from the devcontainer's postCreateCommand) before the labs
introduce real domain code and tests.
"""


def test_placeholder() -> None:
    assert True
